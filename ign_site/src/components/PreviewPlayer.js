import React from "react";

import { StyledPreviewPlayer } from './styles/PreviewPlayer.styled';

export default class PreviewPlayer extends React.Component {

    render() {
        return(<StyledPreviewPlayer>

        </StyledPreviewPlayer>);
    }
}